<%= form_with(model: hut, local: true, class: 'form-horizontal') do |form| %>
  <% if hut.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hut.errors.count, "error") %> prohibited this hut from being saved:</h2>

      <ul>
      <% hut.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, class: 'col-md-1 title' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :level, class: 'col-md-1 title' %>
    <%= form.number_field :level, class: 'form-control'  %>
  </div>

  <div class="form-group">
    <%= form.submit '保存', class: 'btn btn-primary' %>
    <%= link_to '返回', huts_path, class: 'btn btn-info'%>
  </div>
<% end %>
