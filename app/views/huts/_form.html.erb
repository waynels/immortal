<%= form_with(model: hut, local: true) do |form| %>
  <% if hut.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hut.errors.count, "error") %> prohibited this hut from being saved:</h2>

      <ul>
      <% hut.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
